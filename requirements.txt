# Runtime dependencies for the deployed services only.
# Notebooks, dbt, MLflow, DVC → requirements-dev.txt
#
# starlette<1.4 is REQUIRED: starlette 1.4.0 made GZipResponder's
# thread_minimum_size keyword-only, which crashes streamlit's
# starlette_gzip_middleware on every request. Do not loosen without
# confirming streamlit has shipped a fix.

# --- Streamlit app (src/streamlit_app.py) ---
streamlit>=1.37,<1.59
starlette<1.4
plotly>=5.20

# --- FastAPI service (src/api/main.py, via Procfile) ---
fastapi>=0.111
uvicorn>=0.30
pydantic>=2.7
joblib>=1.3
scikit-learn~=1.4.0

# --- shared ---
pandas>=2.0
numpy>=1.26,<3
